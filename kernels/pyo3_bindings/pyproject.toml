[build-system]
requires      = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name             = "kernelserve-ext"
version          = "0.1.0"
requires-python  = ">=3.11"

[tool.maturin]
module-name = "kernelserve_core"
features    = ["pyo3/extension-module"]
